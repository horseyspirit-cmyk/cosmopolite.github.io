# Календарь событий 2026

Статический веб-сайт для отображения календаря событий на 2026 год.

## 🚀 Быстрый старт

Просто откройте `index.html` в браузере или запустите локальный сервер:

```bash
# Python 3
python -m http.server 8000

# Node.js (npx)
npx serve

# PHP
php -S localhost:8000
```

Затем откройте http://localhost:8000 в браузере.

> ⚠️ **Важно:** Из-за политики CORS, загрузка `events.json` через `fetch()` требует запуска через локальный сервер. Прямое открытие HTML-файла может не работать в некоторых браузерах.

## 📁 Структура файлов

```
/
├── index.html      # Основной файл приложения
├── events.json     # Данные о событиях
└── README.md       # Документация
```

## 📅 Формат данных events.json

### Структура файла

```json
{
  "events": [...],
  "categories": [...]
}
```

### Событие (Event)

| Поле | Тип | Обязательное | Описание |
|------|-----|--------------|----------|
| `id` | string | ✅ | Уникальный идентификатор события |
| `title` | string | ✅ | Название события |
| `startDate` | string | ✅ | Дата начала в формате `YYYY-MM-DD` |
| `endDate` | string | ✅ | Дата окончания в формате `YYYY-MM-DD` |
| `color` | string | ✅ | Цвет события в формате HEX (`#RRGGBB`) |
| `category` | string | ✅ | ID категории события |
| `location` | string | ❌ | Место проведения |
| `description` | string | ❌ | Подробное описание события |
| `url` | string | ❌ | Ссылка на внешний ресурс |

#### Пример события

```json
{
  "id": "conf-2026-03",
  "title": "DevOps Conference",
  "startDate": "2026-03-05",
  "endDate": "2026-03-07",
  "color": "#3b82f6",
  "category": "conference",
  "location": "Казань",
  "description": "Конференция по DevOps практикам, CI/CD, Kubernetes и облачным технологиям.",
  "url": "https://devopsconf.example.com"
}
```

### Категория (Category)

| Поле | Тип | Обязательное | Описание |
|------|-----|--------------|----------|
| `id` | string | ✅ | Уникальный идентификатор категории |
| `name` | string | ✅ | Отображаемое название категории |
| `color` | string | ✅ | Цвет категории в формате HEX |

#### Пример категории

```json
{
  "id": "conference",
  "name": "Конференции",
  "color": "#3b82f6"
}
```

## ➕ Как добавить новое событие

1. Откройте файл `events.json` в текстовом редакторе
2. Добавьте новый объект в массив `events`:

```json
{
  "id": "my-event-2026",
  "title": "Моё событие",
  "startDate": "2026-06-15",
  "endDate": "2026-06-15",
  "color": "#8b5cf6",
  "category": "workshop",
  "location": "Онлайн",
  "description": "Описание моего события",
  "url": "https://example.com"
}
```

3. Сохраните файл и обновите страницу в браузере

### Многодневные события

Для создания многодневного события укажите разные даты в `startDate` и `endDate`:

```json
{
  "id": "long-event",
  "title": "Недельный интенсив",
  "startDate": "2026-07-06",
  "endDate": "2026-07-12",
  ...
}
```

Событие будет отображаться как горизонтальная полоса через все дни.

## 🏷️ Как добавить новую категорию

1. Откройте `events.json`
2. Добавьте новый объект в массив `categories`:

```json
{
  "id": "webinar",
  "name": "Вебинары",
  "color": "#06b6d4"
}
```

3. Используйте `"category": "webinar"` в событиях

## 🎨 Рекомендуемые цвета

| Категория | Цвет | HEX |
|-----------|------|-----|
| Конференции | 🔵 Синий | `#3b82f6` |
| Дедлайны | 🔴 Красный | `#ef4444` |
| Воркшопы | 🟣 Фиолетовый | `#8b5cf6` |
| Митапы | 🟠 Оранжевый | `#f59e0b` |
| Праздники | 🟢 Зелёный | `#10b981` |
| Вебинары | 🔵 Голубой | `#06b6d4` |
| Релизы | 🟡 Жёлтый | `#eab308` |
| Обучение | 🩷 Розовый | `#ec4899` |

## 🔗 Шаринг с фильтрами

Календарь поддерживает сохранение состояния в URL:

- **Фильтр по категориям:** `?categories=conference,workshop`
- **Открытый месяц:** `?month=3` (март)
- **Комбинация:** `?categories=conference&month=6`

Скопируйте URL из адресной строки, чтобы поделиться с коллегами.

## 🌙 Тёмная тема

Календарь автоматически определяет системные настройки темы. Также можно переключать тему вручную кнопкой в шапке.

## ⌨️ Горячие клавиши

| Клавиша | Действие |
|---------|----------|
| `Esc` | Закрыть модальное окно |

## 📱 Адаптивность

Календарь корректно отображается на:
- 📱 Мобильных устройствах (1 колонка месяцев)
- 📱 Планшетах (2 колонки)
- 💻 Ноутбуках (3 колонки)
- 🖥️ Мониторах (4 колонки)

## 🛠️ Технологии

- HTML5 + Семантическая разметка
- Tailwind CSS (CDN)
- Vanilla JavaScript (ES6+)
- Никаких зависимостей и сборщиков

## 📄 Лицензия

MIT License - используйте свободно для любых целей.
